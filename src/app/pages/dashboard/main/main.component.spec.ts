/* tslint:disable:no-unused-variable */

import {MainComponent} from './main.component';

describe('Component: Main', () => {
  it('should create an instance', () => {
    let component = new MainComponent(null, null);
    expect(component).toBeTruthy();
  });
});
